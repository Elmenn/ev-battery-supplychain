---
phase: 14-order-based-private-quantity-proofs
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - backend/api/server.js
  - backend/api/db.js
  - zkp-backend/src/main.rs
  - zkp-backend/src/zk/equality_proof.rs
  - zkp-backend/src/zk/quantity_total_proof.rs
autonomous: false
requirements: []

must_haves:
  truths:
    - "Backend APIs gain order-scoped read/write routes keyed by orderId"
    - "Proof binding uses a stable contextHash shared across storage and verifier paths"
    - "Phase 14 proof bundle contains quantity-total proof plus total-payment equality proof"
    - "Amount-critical proof interfaces stop accepting JS Number semantics for wei-scale values"
    - "Legacy equality endpoints remain available until V2 frontend rollout is complete"
  artifacts:
    - path: "backend/api/server.js"
      provides: "order and order-attestation REST routes"
      contains: "orderId"
    - path: "zkp-backend/src/zk/quantity_total_proof.rs"
      provides: "quantity-total relation proof module"
      contains: "prove_quantity_total"
    - path: "zkp-backend/src/main.rs"
      provides: "V2 proof endpoints"
      contains: "generate-quantity-total-proof"
---

<objective>
Implement the V2 backend layer: order-scoped APIs, canonical context hashing, quantity-total proof endpoints, and total-payment equality proof V2 binding.

Purpose: Phase 14 cannot move into frontend integration until the backend can store and verify per-order attestations and proof bundles safely.
Output: order-aware REST endpoints and proof endpoints ready for frontend wiring.
</objective>

<execution_context>
@C:/Users/yamen/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/yamen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-order-based-private-quantity-proofs/14-CONTEXT.md
@.planning/phases/14-order-based-private-quantity-proofs/14-RESEARCH.md
@.planning/phases/14-order-based-private-quantity-proofs/14-01-SUMMARY.md

@backend/api/server.js
@backend/api/db.js
@zkp-backend/src/main.rs
@zkp-backend/src/zk/equality_proof.rs
</context>

<interfaces>
Recommended route families:

```text
POST   /orders
GET    /orders/:orderId
PATCH  /orders/:orderId/status
PATCH  /orders/:orderId/vc-cid

POST   /order-attestations
GET    /order-attestations/:orderId
PATCH  /order-attestations/:orderId/proof-bundle
```

Recommended proof endpoints:

```text
POST /zkp/generate-quantity-total-proof
POST /zkp/verify-quantity-total-proof
POST /zkp/generate-total-payment-equality-proof
POST /zkp/verify-total-payment-equality-proof
```

Canonical context hash rule:

```text
contextHash = keccak256(abi.encode(orderId, memoHash, railgunTxRef, productId, chainId, escrowAddr, unitPriceHash))
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add order-scoped REST APIs and sidecar persistence</name>
  <files>
    backend/api/server.js
    backend/api/db.js
  </files>
  <action>
Add the order-scoped backend API surface needed by the V2 frontend.

Required outcomes:
- CRUD-style routes for `product_orders`
- read/write routes for `order_private_attestations`
- persistence of:
  - `orderId`
  - `memoHash`
  - `railgunTxRef`
  - `unitPriceHash`
  - `contextHash`
  - order VC CID/hash
  - proof bundle JSON

Guardrails:
- lower-case address inputs before storage
- leave legacy `/metadata` and `/buyer-secrets` intact
- define route contracts so frontend can run DB-first with legacy fallback
  </action>
  <verify>
    API tests cover order creation, fetch, status update, VC CID update, and proof bundle patch by `orderId`.
  </verify>
  <done>
    - order APIs exist and are schema-aligned
    - sidecar records are keyed by `orderId`
  </done>
</task>

<task type="auto">
  <name>Task 2: Add quantity-total proof endpoints and V2 equality binding</name>
  <files>
    zkp-backend/src/main.rs
    zkp-backend/src/zk/equality_proof.rs
    zkp-backend/src/zk/quantity_total_proof.rs
  </files>
  <action>
Implement the V2 proof backend.

Required outcomes:
- add a new quantity-total proof module
- add proof generate/verify endpoints for that module
- add V2 total-payment equality endpoints or adapt existing equality endpoints for explicit `contextHash`
- document or encode the exact public inputs and witness split
- stop relying on `u64` + JS Number assumptions for wei values entering the proof API

Important:
- keep legacy equality endpoints available while V2 is not fully rolled out
- reject context mismatches rather than silently normalizing them
  </action>
  <verify>
    Proof tests cover:
    - valid `total = unitPrice * quantity`
    - invalid quantity
    - invalid total
    - invalid context hash
    - valid total-payment equality
    - mismatched payment commitment
  </verify>
  <done>
    - V2 proof bundle backend exists
    - context binding is canonical and explicit
  </done>
</task>

</tasks>

<verification>
Run backend API tests and ZKP backend tests. Verify that:
- proof generation succeeds with large decimal-string wei inputs
- wrong `contextHash` fails verification
- legacy equality routes still respond
</verification>

<success_criteria>
- order-scoped REST routes exist and persist by `orderId`
- quantity-total proof endpoints exist and verify correctly
- total-payment equality proof uses V2 context binding
- amount interfaces no longer depend on JS `Number`
- legacy proof and metadata routes remain available
</success_criteria>

<output>
After completion, create `.planning/phases/14-order-based-private-quantity-proofs/14-02-SUMMARY.md`
</output>
